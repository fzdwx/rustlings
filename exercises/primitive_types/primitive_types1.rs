// primitive_types1.rs
// 填入剩下的有代码缺失的行 没有提示，也没有什么技巧，只要习惯于打这些字就可以了 :)


fn main() {
    // Booleans (`bool`)

    let is_morning = true;
    if is_morning {
        println!("Good morning!");
    }

    let is_evening = true;   // 像例子一样完成这一行的其余部分! 或者让它成为假的!
    if is_evening {
        println!("Good evening!");
    }
}
